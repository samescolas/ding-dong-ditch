# Optional: password-protect the Ring management UI
# Leave empty for no password (local-only use)
#UI_PASSWORD=

# Storage backend: "local" (default) or "s3"
#STORAGE_BACKEND=local

# S3 configuration (only needed when STORAGE_BACKEND=s3)
# Works with AWS S3, MinIO, Backblaze B2, Cloudflare R2
#S3_BUCKET=my-recordings
#S3_REGION=us-east-1
#S3_ENDPOINT=https://s3.amazonaws.com
#S3_ACCESS_KEY_ID=
#S3_SECRET_ACCESS_KEY=
#S3_PREFIX=dingdongditch/

# MQTT integration (for Home Assistant)
# Publishes recording events and auto-discovers sensors in HA
#MQTT_ENABLED=true
#MQTT_BROKER=mqtt://emqx:1883
#MQTT_TOPIC_PREFIX=dingdongditch
#MQTT_DISCOVERY_PREFIX=homeassistant

# AI Vision Description (optional)
# When enabled, snapshots are sent to an OpenAI-compatible vision API
# and the description is included in MQTT recording events.
#AI_ENABLED=false
#AI_API_URL=http://localhost:8080/v1
#AI_API_KEY=
#AI_MODEL=gpt-4o
#AI_PROMPT=Describe what you see in this security camera image in one concise sentence.

# Log level: debug, info (default), warn, error
#LOG_LEVEL=info
